import { TeachersPipePipe } from './teachers.pipe';

describe('TeachersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TeachersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
